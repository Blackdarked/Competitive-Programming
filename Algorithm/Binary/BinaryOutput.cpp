#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    cout << "a: " << bitset<16>(a) << " b: " << bitset<16>(b) << " c: " << bitset<16>(c);
    return 0;
}